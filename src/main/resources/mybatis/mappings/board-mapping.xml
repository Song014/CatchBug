<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="BoardDAO">

	<resultMap type="board" id="boardResult">
		<id property="seq" column="SEQ"></id>
		<result property="title" column="TITLE" />
		<result property="write" column="WRITE"/>
		<result property="content" column="CONTENT"/>
		<result property="regDate" column="REGDATE"/>
		<result property="cnt" column="CNT"/>
	</resultMap>
	
	<insert id="insertBoard" parameterType="board">
		<selectKey keyProperty="seq" resultType="int" order="BEFORE">
			select board_seq.nextval as seq from dual
		</selectKey>
		insert into board(seq,title,writer,content)
		values(#{seq}, #{title},#{writer},#{content})  <!--#{}는 vo객체의 getter메소드이다. -->
		<!-- values((select nvl(max(seq),0) + 1 from board), #{title},#{writer},#{content}) -->  <!--#{}는 vo객체의 getter메소드이다. -->
	</insert>
	
	<update id="updateBoard">
		update board set title=#{title},content=#{content} where seq=#{seq}
	</update>
	
	<delete id="deleteBoard">
		delete from board where seq=#{seq}
	</delete>
	
	<select id="getBoard" resultType="board">
		select * from board where seq=#{seq}   <!-- vo객체에 setter 자동주입 -->
	</select>
	
	<select id="getBoardList" parameterType="board" resultMap="boardResult">
		<!-- <![CDATA[
		select * from board 
		where title like '%'||#{searchKeyword}||'%' 
		and seq > 0
		order by seq desc
		]]> -->
		
		select * from board 
		where 1 = 1
		<if test="searchCondition == 'TITLE'">
			AND TITLE LIKE '%'||#{searchKeyword}||'%'
		</if>
		<if test="searchCondition == 'CONTENT'">
			AND CONTENT LIKE '%'||#{searchKeyword}||'%'
		</if>
		order by seq desc
	</select>
</mapper>