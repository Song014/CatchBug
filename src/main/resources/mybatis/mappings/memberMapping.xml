<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MemberDAO">

		<!--  <resultMap type="board" id="boardResult">
			<id property="seq" column="SEQ"></id>
			<result property="title" column="TITLE"/>
		</resultMap> -->
	
	<insert id="insertMember" >
		<!-- <selectKey keyProperty="seq" resultType="int" order="BEFORE">
			select board_seq.nextval as seq from dual;
		</selectKey>
		insert into board(seq,title,writer,content)
		values(#{seq},#{title},#{writer},#{content}) -->
		
		insert into member(id,pass,business_no,business_name,ceo,contact,email,business_address)
			values(#{id},#{pass},#{business_no},#{business_name},#{ceo},#{contact},#{email},#{business_address})
			<!-- values('1','2','3','4','5','6','7','8') -->
			<!-- #{  } 는 vo객체에 자동 setter 주입 -->
	</insert>
	

	<select id="getMember" resultType="member" parameterType="member">
		select * from member where id=#{id} and pass=#{pass}
	</select>
	
	<update id="updateMypage">
		update member set business_no=#{business_no}, business_name=#{business_name}, ceo=#{ceo}, contact=#{contact}, email=#{email}, business_address= #{business_address}  where id=#{id} 
	</update>
 	
	
	<update id="insertImg">
		update member set uploadImgFile=#{updateImgFile}  where id=#{id}
	</update>
	   	<!-- 발주내역에서 id검색하면 id정보 조회 -->
    <select id="getMemberid" resultType="member" parameterType="member">
		select id,business_no,business_name,ceo from member where id=#{id}
	</select>
	
</mapper>